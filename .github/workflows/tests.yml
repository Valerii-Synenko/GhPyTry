name: Tests (Allure TestOps)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    name: Run Tests with Allure TestOps
    runs-on: ubuntu-latest

    steps:
      - name: Checkout test repository
        uses: actions/checkout@v4

      - name: Checkout microservices-demo
        uses: actions/checkout@v4
        with:
          repository: Valerii-Synenko/microservices-demo
          path: microservices-demo

      - name: Docker compose up microservices-demo
        run: |
          cd microservices-demo/deploy/docker-compose
          docker compose up -d
          echo "Waiting for services..."
          sleep 60

      - name: Verify Docker containers
        run: docker ps

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install Poetry
        uses: snok/install-poetry@v1
        with:
          virtualenvs-create: true
          virtualenvs-in-project: true

      - name: Cache Poetry dependencies
        uses: actions/cache@v3
        with:
          path: .venv
          key: venv-${{ runner.os }}-${{ hashFiles('**/poetry.lock') }}

      - name: Install dependencies
        run: poetry install --no-interaction --no-root

      - name: Run pytest and upload results to Allure TestOps
        env:
          ALLURE_ENDPOINT: ${{ secrets.ALLURE_ENDPOINT }}
          ALLURE_TOKEN: ${{ secrets.ALLURE_TOKEN }}
          ALLURE_PROJECT_ID: ${{ secrets.ALLURE_PROJECT_ID }}
        run: |
          poetry run pytest \
            --alluredir=allure-results \
            --clean-alluredir \
            -v

      - name: Upload results to Allure TestOps
        if: always()
        run: |
          curl -fsSL https://github.com/allure-framework/allurectl/releases/latest/download/allurectl_linux_amd64 \
            -o allurectl
          chmod +x allurectl

          ./allurectl upload allure-results \
            --endpoint $ALLURE_ENDPOINT \
            --token $ALLURE_TOKEN \
            --project-id $ALLURE_PROJECT_ID \
            --launch-name "GitHub Actions #${{ github.run_number }}" \
            --launch-tags "github,ci,pytest" \
            --launch-attributes \
              branch=${{ github.ref_name }} \
              commit=${{ github.sha }} \
              actor=${{ github.actor }}

      - name: Docker compose down
        if: always()
        run: |
          cd microservices-demo/deploy/docker-compose
          docker compose down -v